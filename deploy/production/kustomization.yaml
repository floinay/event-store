apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - postgres.yaml
  - kafka.yaml
  - runtime.yaml
  - projection-failure-prune-cronjob.yaml
configMapGenerator:
  - name: event-store-connect-jmx
    files:
      - config.yaml=connect-metrics-config.yaml
